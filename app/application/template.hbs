<div class="app-wrapper">
  <nav>
    <div class="nav-container">
      <div class="nav-row">
        <span>{{#link-to "users" class="fobie"}}FOHBI{{/link-to}}</span>

        <div class="nav-link__group">
          {{#if session.isAuthenticated}}

            {{!-- <h4 class="greeting">
              Hello, {{session.session.content.authenticated.user.username}}.
            </h4> --}}

            <div class="nav-link__container">
              {{#link-to "users"}}
                <span class="nav-link__text">Home</span>
                <button class="nav-link">{{fa-icon "home"}}</button>
              {{/link-to}}
            </div>

            <div class="nav-link__container">
              {{#link-to "logout"}}
                <span class="nav-link__text">Log Out</span>
                <button class="nav-link">{{fa-icon "sign-out"}}</button>
              {{/link-to}}
            </div>

          {{/if}}
        </div>

      </div>
    </div>
  </nav>

  {{outlet}}
</div>
