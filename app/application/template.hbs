<div class="app-wrapper">
  <nav>
    <div class="container">
      <div class="nav-row">
        <span>{{#link-to "admin" class="nav-link"}}{{fa-icon "home"}}{{/link-to}}</span>
        <span>{{#link-to "admin" class="fobie"}}FOHBI{{/link-to}}</span>
        {{#if session.isAuthenticated}}
          {{#link-to "logout" class="nav-link"}}{{fa-icon "sign-out"}}{{/link-to}}
        {{else}}
          {{#link-to "login" class="nav-link"}}{{fa-icon "sign-in"}}{{/link-to}}
        {{/if}}
      </div>
    </div>
  </nav>

  {{outlet}}
</div>
